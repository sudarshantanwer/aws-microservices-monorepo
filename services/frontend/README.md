Frontend dev server

Install deps:

npm install

Run dev server:

npm start

The Vite dev server proxies /auth, /users, /products, /orders, /payments to the deployed API Gateway endpoints for local development.
